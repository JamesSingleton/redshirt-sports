# Sanity Studio Vite prebundles import these as bare specifiers; pnpm
# otherwise keeps them nested under `sanity` and resolution fails.
public-hoist-pattern[]=*i18next*
public-hoist-pattern[]=*react-i18next*
